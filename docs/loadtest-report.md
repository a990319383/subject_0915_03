# 门店底账 180 并发压测报告

日期：2026-09-15 ｜ 环境：4 vCPU / 8 GB，Java 8 (Corretto 1.8.0_504)，Spring Boot 2.7.18 + H2 文件库
数据规模：工单主表 85,000 行种子（明细 170,000 行、流转留痕 ~27 万行、扣次记录 46,753 行）
工具：`scripts/loadtest.py`（Python 标准库，无线程池外依赖）

## 第一轮：工单建档（写）

```
python3 scripts/loadtest.py --mode create --concurrency 180 --requests 3600
```

| 指标 | 结果 |
|---|---|
| 并发 | 180 |
| 总请求 | 3,600 |
| 成功 / 失败 | 3,600 / 0 |
| 吞吐 | 266.2 QPS |
| 平均时延 | 636.9 ms |
| p50 / p95 / p99 / max | 543.7 / 1,564.8 / 2,230.5 / 2,924.1 ms |

每单 = 主表 1 行 + 明细 2 行 + 建档留痕 1 行，同事务落库；请求号/操作人/业务时间/数据版本随行写入。

## 第二轮：按工单号检索（读）

```
python3 scripts/loadtest.py --mode query --concurrency 180 --requests 18000
```

| 指标 | 结果 |
|---|---|
| 并发 | 180 |
| 总请求 | 18,000 |
| 成功 / 失败 | 18,000 / 0 |
| 吞吐 | 591.0 QPS |
| 平均时延 | 234.2 ms |
| p50 / p95 / p99 / max | 182.9 / 625.5 / 929.1 / 1,553.7 ms |

检索键 `order_no` 唯一索引，返回主表 + 明细。

## 压测后台账核验（SQL 直查）

| 检查项 | 结果 |
|---|---|
| 工单主表物理行 | 88,604（种子 85,000 + 压测 3,600 + 冒烟 4） |
| 明细行 | 177,205，与主表 1:2 对应 |
| 压测工单明细缺失数 | **0**（180 并发下无半截落库） |
| 软删标记行 | 1（仅打标记，未物理删除） |
| 被拒绝的越级流转留痕 | 2（accepted=N） |
| 反向记录 / 扣次记录 | 4,251 / 46,753 |
| 应用日志错误 | 0 |
